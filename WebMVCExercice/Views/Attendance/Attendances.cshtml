@model IEnumerable<WebMVCExercice.ViewModels.StudentAttendanceViewModel>

@{
    ViewBag.Title = "Attendances";
}

<h2>Attendances</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StudentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateAttendance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsPresentAttendance)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StudentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateAttendance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsPresentAttendance)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
